<div>
  <TestBlock tagName={tag} {data}={colors.0} />
  <TestComponent id="colors" class="box">
    <TestBlock data={colors.1} />
    <a href="/colors">Read More</a>
  </TestComponent>
  <Each data={colors}>
    <TestBlock data={@item} />
    <Defined data={@item.slug}>
      <a href="/colors/{@item.slug}">Read More</a>
    </Defined>
  </Each>
  <script src={scripts.main}>
    function hello(str) { }
  </script>
</div>

